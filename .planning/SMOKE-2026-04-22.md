# Smoke Checklist — 2026-04-22

**Live site:** https://thetiniestspoon.github.io/mykeepsakes/

Covers four commits pushed today, mid-Sankofa:
- `ef3c767` — Event FAB option + long-press-to-delete
- `ba2ea1b` — photo-scoping fix + upload UX + Directions removal
- `b2212f0` — QuickAddButton removed + photos cache scoped to active trip
- `acf0450` — Phase 4d Collage migration (LocationDetail, PhotoDetail, PackingDetail)

Walk these once the GH Actions deploy for `acf0450` finishes.

## A. Photo scoping (the bug fix)

1. Log in with your PIN. Pick an event.
2. Open the event detail → tap the **Camera** button → `Add Memory` dialog opens.
3. Tap **Add** inside the dialog. OS picker should show **both library and camera** options — no camera-only lock.
4. Pick a photo from your library → Save.
5. Confirm the new photo appears on this event.
6. Navigate to a **different event at the same location** (e.g., another session in the same conference room).
7. Confirm the new photo does **NOT** appear on that other event. ✅ = fix works.
8. Legacy memories captured before today (null `itinerary_item_id`) should NOT appear on any event — only in the Album tab. ✅ = strict-scoping as intended.

## B. Upload UX

9. In MemoryCaptureDialog: upload a **video** from library → confirm it saves and plays back (media_type = video).
10. Open a Reflection (PenLine FAB button). Tap the photo button → OS picker shows library + camera. Upload an image → saves. Upload a video → saves (Reflection now auto-detects media type).

## C. Get Directions removal

11. On an event detail's icon action row (top of ActivityDetail), confirm you see: Check, ♥, Camera, Pencil, MapPin.
12. The **Route** (Get Directions) icon should be **gone**. ✅
13. On a **location** detail (not event), "Get Directions" button should still be present — scope decision, intentional.
14. In the full-screen Map modal, the Get Directions dropdown (Google / Apple / Waze) should still be present — also intentional.

## D. Event FAB + long-press delete

15. Tap the `+` FAB → expands into three pill buttons: **Reflection**, **Connection**, **Event**.
16. Tap **Event** → a bottom sheet opens with title / day / category. Fill it in, Save. New item should appear on the selected day's itinerary.
17. On mobile: touch-and-hold an itinerary event for ~650ms. Confirm dialog appears. Cancel dismisses; Delete removes. ✅
18. On desktop: mouse-down and hold on an event for ~650ms (don't move cursor). Same confirm dialog appears.
19. Swipe-left menu on mobile should still work (Edit / Skip / Memory / Delete) — coexistence with long-press.
20. Confirm only ONE FAB on the itinerary view — `QuickAddButton` removed.

## E. Phase 4d panels (new visual look)

21. **LocationDetail** — opens when you pick a location. Should feel paper-flat: sharp corners, crème/ink colors, a small rotated yellow Tape strip on the header card, an outline rubber-stamp "Location" (or "Stay") label, and a pen-blue sticker pill for the category badge. Action buttons are ghost-style, not rounded.
22. **PhotoDetail** — opens when reviewing a memory. The main photo is framed as a tilted **polaroid** with tape and a handwritten caption. Multiple photos stagger at different small rotations. A Caveat ("kept") note peeks from the note card.
23. **PackingDetail** — ink rubber-stamp category headers in uppercase (Rubik Mono One). Count "3 of 5" appears as a pen-blue sticker pill. A single Tape strip top-left over the card. Progress bar is a flat 4px pen-blue line, not a rounded shadcn bar. A tape-yellow rotated icon tile replaces the old cream circle at top.

## Known NOT-done this session

- `ActivityDetail` — still has its old Beach-style layout (tokens already swapped). Deliberately held back until this smoke checklist clears, to avoid stacking layout changes on top of today's bug fix.
- `StayDetail` / `DefaultCenterContent` — lower visual-per-effort, deferred.
- `GuideDetail` / `AlbumExperience` — dispatchers; blocked on Phase 4 #8 (GuideTab) and #5 (AlbumTab) respectively.

## If something's broken

1. First: is GH Actions green? Open the Actions tab on the repo.
2. If the deploy's green but a fix regressed, the most likely suspects per commit:
   - Photo not appearing under event: check commit `ba2ea1b` — `useItemMemories` wiring.
   - Library option missing: commit `ba2ea1b` — the `capture="environment"` removals.
   - Route icon still visible: commit `ba2ea1b` — ActivityDetail Tooltip removal.
   - Weird stray button at bottom-right: commit `b2212f0` — QuickAddButton render removal.
   - Panel chrome looks broken: commit `acf0450` — one of the three migrated DetailPanels.
3. Revert via `git -C "<path>" revert <hash>` + push. No force-push to main.

— Generated after /l add on 2026-04-22. Markdown version of the dashboard smoke checklist.
