# Smoke Checklist — 2026-04-23

**Live site:** https://thetiniestspoon.github.io/mykeepsakes/

Covers three commits pushed today, mid-Sankofa, all driven by yesterday's SMOKE doc findings:
- `c1484f7` — MemoryCaptureDialog scrolls + input reset for chained picks + Delete button on ActivityDetail + drop broken long-press
- `fc6caa7` — Add Event sheet reaches full field parity with existing events
- `e4026c4` — PhotoViewer Edit + Delete; retroactive day/location/event tagging on memories

Walk these once GH Actions has deployed `e4026c4`. Yesterday's doc (SMOKE-2026-04-22.md) already passed 8/9 — its one failure (D.17-18 long-press) is resolved by `c1484f7` below.

## A. MemoryCaptureDialog — scroll + chained picks (`c1484f7`)

1. Open any event → Camera → `Add Memory` dialog.
2. On a narrow/short viewport: confirm the dialog scrolls to Save button instead of clipping.
3. Tap "Add" → pick a photo → dialog previews it. Tap "Add" again → picker opens again reliably. Repeat 2–3 times. On iOS Safari this should now chain cleanly (was failing yesterday because the input value wasn't resetting).

## B. Delete button on ActivityDetail (`c1484f7`)

4. Open an event detail → the icon action row should show: Check, ♥, Camera, Pencil, **Trash2 (red-tinted)**, MapPin.
5. Tap the Trash2 icon → AlertDialog: "Delete this event? \"[title]\" will be removed…". Cancel → no-op. Delete → event is removed from the itinerary.

## C. Long-press no longer glitches (`c1484f7`)

6. On mobile or desktop, touch-and-hold / click-and-hold any itinerary event card for 1–2 seconds. Should now behave like a normal tap-hold (maybe text selection, depending on browser) — no confirm dialog, no phantom Delete action. The broken long-press is fully gone.
7. Mobile swipe-left on an event card should still open the Edit/Skip/Memory/Delete menu. (Swipe-menu Delete is the mobile-only delete path; the new explicit Delete button is the cross-platform one.)

## D. Add Event sheet field parity (`fc6caa7`)

8. Tap the `+` FAB → Event → sheet opens. Fields should now be: **Title*** + **Time**, **Day** + **Category**, **Location**, **Description**, **Link + Link Label**, **Phone**, **Notes**. Sheet scrolls if the form is long.
9. Fill in Title + Time + pick a Location from the dropdown + Description. Save.
10. New event appears on the selected day **at the correct time slot** (not at the bottom anymore, since `start_time` is set).
11. Tap the event → the MapPin icon should work (because `location_id` is set).
12. Tap Edit (Pencil) on the event → DatabaseActivityEditor opens with all fields round-tripped: title, time, description, link, phone, notes. Location isn't on that editor either but it's stored correctly.

## E. PhotoViewer Edit + Delete (`e4026c4`)

13. Album tab → tap any photo → full-screen viewer opens.
14. Footer should show: Edit (pencil icon), Download, Share (mobile only when `navigator.share` exists), Delete (trash, red-tinted) — plus Page "X of Y" and thumbnails.
15. Tap **Edit** → `MemoryEditDialog` opens. Fields: Title, Note, Day, Location, Event. The current memory's existing tags should be pre-filled. Pick a new Location + Event → Save.
16. Verify the memory now shows up under the newly-tagged event's detail view (retroactive tagging works end-to-end).
17. Tap **Delete** on a different photo → AlertDialog "Delete this photo?" → Delete. Photo is removed; if the memory had other photos, they remain in album.
18. If deleting the last photo in the viewer, the viewer closes automatically.

## F. PhotoViewer Edit/Delete from event detail (`e4026c4`)

19. Open an event with photos → tap a photo strip thumbnail → full-screen viewer opens.
20. Same Edit + Delete buttons appear. Edit opens MemoryEditDialog; Delete calls useDeleteMemoryMedia.
21. Confirm: photos shown in the event viewer are item-scoped (not cross-event bleed) — that's yesterday's fix still holding.

## Known NOT-done (deferred post-Sankofa)

- `ActivityDetail` layout is still Beach-style (tokens already swapped). Collage-primitive migration of the layout is queued as a `/gsd:quick` post-conference.
- `StayDetail` + `DefaultCenterContent` Phase 4d polish.
- `GuideDetail` + `AlbumExperience` (dispatchers — blocked on Phase 4 #8 and #5 downstream).
- Phase 4 #4–#11 (Memory / Album / Map / People / Guide / Lodging / Reflection+Connection / Shared-Trip).

## If something's broken

1. GH Actions green? https://github.com/thetiniestspoon/mykeepsakes/actions
2. Likely-suspects per commit:
   - Dialog overflows / library pick fails on iOS → `c1484f7` (MemoryCaptureDialog changes)
   - Event missing fields or saves to wrong day → `fc6caa7` (sheet form handler)
   - Edit/Delete buttons missing in viewer → `e4026c4` (PhotoViewer prop wiring)
   - Retroactive tag doesn't round-trip → `e4026c4` (MemoryEditDialog's NONE sentinel or useUpdateMemory field set)
3. Revert via `git -C "<path>" revert <hash>` and push. No force-push to main.

— Companion to SMOKE-2026-04-22.md. Written after today's /l add wrap.
